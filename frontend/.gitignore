# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dev-only manual copy of data/dashboard/timeseries.json (see PriceRegimeChart.jsx comment) —
# not committed since it duplicates data already tracked there; a build-time copy step replaces
# this manual step later.
public/timeseries.json

# Dev-only manual copy of data/dashboard/montecarlo.json (see MonteCarloChart.jsx comment) —
# same treatment as public/timeseries.json above.
public/montecarlo.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
